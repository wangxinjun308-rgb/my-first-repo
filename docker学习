docker学习笔记
概念理解：docker是一个可以把运行环境（比如代码、系统工具、库等）一起打包成一个独立的集装箱的容器
优点：占用内存小 启动速度快 占用资源小 资源利用率高 安全性高
三要素：镜像：一个只读的模板，类似于集装箱的“设计图纸”。它包含运行一个应用程序所需的一切（代码、运行时、系统工具、库等）
              容器：从镜像创建的运行实例，就像按照“图纸”造出来的“集装箱”它是可启动、停止、删除的
              仓库：集中存放镜像文件的地方，最著名的是官方的 Docker Hub
功能：统一开发/测试/生产环境
           微服务架构
           持续集成/持续部署
           快速搭建开发环境
在docker中进行的操作：
更新软件源列表
安装 curl 工具
验证 curl 版本
访问百度首页
查看返回内容

镜像管理：
查看本地镜像
docker images
搜索镜像
docker search nginx
拉取镜像
docker pull nginx
删除镜像
docker rmi hello-world

容器管理：
查看正在运行的容器
docker ps
查看所有容器（包括已停止的）
docker ps -a
运行一个交互式容器
docker run -it ubuntu bash
停止容器
docker stop <容器ID或名称>
删除容器
docker rm <容器ID或名称>
